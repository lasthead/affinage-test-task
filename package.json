{
  "name": "affinage",
  "version": "1.0.0",
  "description": "test task",
  "author": "denisbondarenko",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "stylelint": "prettier -c './**/*.scss'"
  },
  "lint-staged": {
    "*.{js,vue}": [
      "eslint --fix",
      "git add"
    ],
    "*.{js,scss}": [
      "prettier --write",
      "git add"
    ]
  },
  "dependencies": {
    "less": "^3.10.3",
    "less-loader": "^5.0.0",
    "nuxt": "^2.11.0",
    "swiper": "^4.5.1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.8.0",
    "eslint-plugin-nuxt": "^0.5.0",
    "eslint-plugin-prettier": "^3.1.2",
    "prettier": "^1.19.1",
    "lint-staged": "^9.5.0"
  }
}
